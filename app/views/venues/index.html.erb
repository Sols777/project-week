<h1>Venues</h1>
<div class="container d-flex cardVenue">

        <% @venues.each do |venue| %>
          <div class="div"><%= render "shared/card", venue: venue %></div>
        <% end %>

</div>

<%= link_to "+ Add a new venue", new_venue_path, class: "btn btn-primary" %>
